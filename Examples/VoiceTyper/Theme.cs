using System.Drawing;

namespace VoiceTyper
{
	/// <summary>Shared accent colours (used when drawing the tray/app icon).</summary>
	static class Theme
	{
		public static readonly Color Accent = Color.FromArgb( 124, 92, 255 ); // violet, matches the WPF theme
	}
}
